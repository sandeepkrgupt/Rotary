import { PModel } from '../product.model';
export function posts (defaultStore:any = [], action:any) {
    switch(action.type) {
        default:
            return defaultStore
    }
    return defaultStore;
}